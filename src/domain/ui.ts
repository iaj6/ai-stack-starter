// ── SWAP POINT (client-safe) ────────────────────────────────────────────────
// Point this at your domain pack's UI config. Imported by client components, so
// keep it free of seed data, prompts, and tool logic.
export { creUI as DOMAIN_UI } from "./packs/cre/ui";
export type { DomainUI } from "./types";
